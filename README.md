
# iceworks-next

## Develop

install deps:

```bash
$ yarn run install:deps
# build packages and run link
$ npm run setup

# start electron app
$ yarn run app:start

# packages
$ yarn run packages:watch
$ yarn run packages:build
```